// Set (unique values)
let set = new Set([1,2,2,3]); // {1,2,3}

// Add / Check
set.add(4);
set.has(2);

// Map (key-value)
let map = new Map();
map.set("a", 1);
map.get("a");

// Object keys/values
Object.keys(obj);
Object.values(obj);