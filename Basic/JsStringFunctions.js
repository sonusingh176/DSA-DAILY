// Length
str.length;

// Convert to array
str.split("");

// Join
arr.join("");

// Lower / Upper
str.toLowerCase();
str.toUpperCase();

// Check substring
str.includes("abc");

// Replace
str.replace("a", "b");

// Char access
str.charAt(0);
str[0];